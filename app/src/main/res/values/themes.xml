<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Window background matches the app's own background token so there is no flash before Compose
         draws. Platform theme parent on purpose: the Material3 theme parent needs the Material
         Components library, and all styling here comes from WifiLensTheme in Compose. -->
    <style name="Theme.Wifilens" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:colorBackground">@color/window_background</item>
    </style>
</resources>
